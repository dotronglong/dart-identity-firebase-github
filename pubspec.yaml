name: identity_firebase_github
description: Github SSO by Firebase for Identity package
version: 0.3.0
homepage: https://github.com/dotronglong/dart-identity-firebase-github

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  identity: ^0.3.0
  identity_firebase: ^0.3.0
  firebase_auth: ^0.15.3
  github_sign_in: ^0.0.1

flutter:
  assets:
    - images/github.png
